    <div class="thumbnail">
      <img src={{submission.image}} alt="...">
      <div class="caption">
        <h3>{{submission.id}}</h3>
        <p>{{submission.rating}}</p>
        <p><a href="#" class="btn btn-primary" role="button">Comment</a> <a href="#" class="btn btn-default" role="button">Reject</a></p>
      </div>
    </div>
